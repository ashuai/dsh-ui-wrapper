# v0.1.4

- 新增:**右键菜单整体禁用** —— 注入 `contextmenu` preventDefault 用户脚本(WebKit 标准机制,与 Safari 同引擎),右键不再弹出任何菜单;编辑操作走 ⌘X / ⌘C / ⌘V(主菜单快捷键)
- 移除:**失效的 WKUIDelegate 右键自定义代码** —— `contextMenuConfigurationForElement` 在 macOS 上未生效(自定义项不显示、系统项无法移除),整段删除
- 精简:依赖去掉 `block2` 与 `objc2-web-kit/WKContextMenuElementInfo` 特性
- 保留:macOS 主菜单快捷键、聚焦时 WKWebView 首响应者管理、常驻 panic 日志(`~/Library/Logs/DSH-panic.log`)
