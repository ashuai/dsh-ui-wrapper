# v0.1.1

- 修复:打字/中文输入法卡顿 —— 窗口与 WebView 改用 tao + wry 默认 `build()`(与 Tauri 相同的组合),输入事件走正常路径,不再使用子视图模式的次级输入路径
- 保留:v0.1.0 的失焦崩溃修复(tao 的窗口代理可正确容忍 contentView 替换,实测三轮失焦无崩溃)
- 功能不变:Debug 模式(`DSH_DEBUG`/`DSH_LOG`/`DSH_DEVTOOLS`)、`Cmd+R` 刷新、大胖鲸图标、三平台 CI 与自动发版
